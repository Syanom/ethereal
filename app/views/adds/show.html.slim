.add-show-wrap
  .add-show-top
    .add-show-images
      .chosen-image
        = image_tag @add.main_image.url, class: "add-show-image-active"
        = image_tag 'arrow_left.png', class: "add-arrow-left"
        = image_tag 'arrow_right.png', class: "add-arrow-right"
      - unless @add.add_images.empty?
        .add-show-img-select
          = image_tag @add.main_image.url, class: "add-show-image active-image"
          - @add.add_images.order(:draw_order).each do |add_image|
            = image_tag add_image.image.url, class: "add-show-image"
    .add-show-properties
      - if @add.header
        .add-show-property.add-header = @add.header
      .add-show-property
        = t('.id') + @add.id.to_s
      .add-show-property
        = t('.date') + @add.created_at.strftime("%d.%m.%Y").to_s
      - if @add.house
        .add-show-property
          = t('.house')
      - if @add.flat
        .add-show-property
          = t('.flat')
      - if @add.for_sale
        .add-show-property
          = t('.for_sale')
      - if @add.for_rent
        .add-show-property
          = t('.for_rent')
      .add-show-property
        = t('.price_tl') + @add.price_tl.to_s.reverse.scan(/.{1,3}/).join(' ').reverse
      - if @add.price_usd
        .add-show-property
          = t('.price_usd') + @add.price_usd.to_s.reverse.scan(/.{1,3}/).join(' ').reverse
      - if @add.price_rub
        .add-show-property
          = t('.price_rub') + @add.price_rub.to_s.reverse.scan(/.{1,3}/).join(' ').reverse
      - if @add.price_eur
        .add-show-property
          = t('.price_eur') + @add.price_eur.to_s.reverse.scan(/.{1,3}/).join(' ').reverse
      - if @add.aidat
        .add-show-property
          = t('.aidat') + @add.aidat.to_s.reverse.scan(/.{1,3}/).join(' ').reverse
      - if @add.site
        .add-show-property = t('.site')
      - if @add.rooms
        .add-show-property
          = t('.rooms') + @add.rooms.to_s + "+" + @add.living_rooms.to_s
      - if @add.number_of_bathrooms
        .add-show-property
          = t('.number_of_bathrooms') + @add.number_of_bathrooms.to_s
      - if @add.square_brut
        .add-show-property
          = t('.square_brut') + @add.square_brut.to_s
      - if @add.square_net
        .add-show-property
          = t('.square_net') + @add.square_net.to_s
      - if @add.building_age  
        .add-show-property
          = t('.building_age') + @add.building_age.to_s
      - if @add.max_floor
        .add-show-property
          = t('.max_floor') + @add.max_floor.to_s
      - if @add.floor
        .add-show-property
          = t('.floor') + @add.floor.to_s
      -if @add.elevator
        .add-show-property
          = t('.elevator')
      - if @add.furnished
        .add-show-property
          = t('.furnished')
      - if @add.credit_available  
        .add-show-property
          = t('.credit_available')
      - if @add.video_call_available
        .add-show-property
          = t('.video_call_available')
      - if @add.description
        .add-show-property 
          .add-show-title = t('.description')
          = simple_format(@add.description)
  //- if @add.description
  //  .add-show-description = simple_format(@add.description)